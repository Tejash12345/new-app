// Single source of truth for the app version shown in the UI.
// Keep in sync with the Flutter app's pubspec.yaml `version:` (name+build),
// e.g. APP_VERSION '1.7.24' ↔ pubspec `1.7.24+50`. Bump on every APK release.
export const APP_VERSION = '1.7.68'
